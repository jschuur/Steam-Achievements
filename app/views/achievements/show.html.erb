<% title 'Achievement Lookup' unless params[:user] %>

<%= render 'form' %>

<% if params[:user] && params[:game] %>
  <%= coffee_script_tag do %>
    $ ->
      load_achievements '<%= params[:user] %>', '<%= params[:game] %>'
  <% end %>
<% end %>