<% friends.each do |friend| %>
  <% user = friend['custom_url'] || friend['steamid'] %>
  <li> <%= link_to(friend['personaname'], "#", {:class => 'friendlink', "data-user" => user }) -%>
  <%= " (#{friend['realname']})" if friend['realname'] && friend['realname'].length > 0 -%>
<% end %>